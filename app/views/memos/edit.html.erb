<div class="container-fluid vh-100">
  <div class="row h-100">

    <!-- ナビゲーションパーシャルを読み込む -->
    <%= render 'shared/navbar' %>

    <div class="col-md-10">
      <h1>Edit Memo</h1>

      <%= render 'form', memo: @memo %>

      <%= link_to 'Back to Memos', folder_memos_path(@folder) %>
    </div>
  </div>
</div>