<div class="calendar-back">
<div class="icon-text">
<i class="fas fa-pencil-alt fa-2x "style="color: #3e3e3e;"></i>
 <p class="learning-title">学習記録</p>
</div>
<hr>
<%= month_calendar do |date| %>
  <%= date.day %>
 
  <% if @memos_by_date[date] %>
    <% @memos_by_date[date].each do |daily_creation| %>
      <p>作成したメモ数: <%= daily_creation.memos_count %></p>
    <% end %>
  <% end %>
<% end %>
<div>